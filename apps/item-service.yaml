apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: item-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Shareify-Project/shareify-item-service.git
    targetRevision: main
    path: charts/item-service
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: shareify-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
